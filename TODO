- Clean up types (especially frontend)
- Current round != last added round - dynamically via timestamps
- RNG? at least for map selects
- Match = Class, Round = Class, to be manipulated from everywhere
